/** Default page number */
export const DEFAULT_PAGE = 1;

/** Default items per page */
export const DEFAULT_LIMIT = 10;

/** Minimum page number */
export const MIN_PAGE = 1;

/** Maximum allowed page size */
export const MAX_PAGE_SIZE = 100;

/** Default cache TTL in seconds */
export const DEFAULT_CACHE_TTL = 60;

/** Maximum cache TTL in seconds (1 hour) */
export const MAX_CACHE_TTL = 3600;

/** Default request timeout in milliseconds */
export const DEFAULT_TIMEOUT_MS = 30_000;

/** Maximum string length for slugs */
export const MAX_SLUG_LENGTH = 255;

/** Default random string length */
export const DEFAULT_RANDOM_STRING_LENGTH = 32;

/** Bcrypt salt rounds */
export const BCRYPT_SALT_ROUNDS = 12;

/** Health check memory heap threshold in bytes (256MB) */
export const HEAP_MEMORY_THRESHOLD = 256 * 1024 * 1024;

